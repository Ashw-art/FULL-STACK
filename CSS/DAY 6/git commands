# git clone "the_other_repo"

####################################################
# Initialize or create the folder
git init 

# Status of files i.e. staged or unstaged

git status

# config of email and username
git config user.name "ahsfhsg"
git config user.email " ajgdh@gmail.com"

# staging the files
git and filename, filename 2
or
git add .

# One-time step (adding remote to your git)
git remote add origin "http://url_of_your_git_repo.git "

# commit the changes (important)
git commit -m "any message starting for what this commit is"

# choose branch
git checkout master
or
git checkout -b new_branch_name

# Push the changes
git push origin master or branch_name

##########################
# pull content from branch
git pull origin master

# merge the changes in current branch
git merge

##############
# fetch all the tree structure from remote
git fetch